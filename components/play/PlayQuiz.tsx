import Carousel from "./Carousel"

const PlayQuiz = (props: {id: string}) => {
  return (
    <div>
      <Carousel>
        <div>Hello</div>
      </Carousel>
    </div>
  )
}

export default PlayQuiz